//write an if statement that checks if a given string is empty. If it is, print "String is Empty", otherwise, do nothing. 
//'empty' and 'null' are different. 

let str = "";
if (str.length === 0) {
  console.log("string is empty.");
}